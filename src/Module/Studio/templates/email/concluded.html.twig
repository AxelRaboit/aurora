{# Both parties, same message: it is done.

   No PDF attached yet - the mailer has no attachment parameter, which is one
   of the things phase 06 adds. The mail says the document is available rather
   than pretending to carry it. #}
<p>{{ 'studio.email.concluded.body'|trans({'{reference}': contract.reference}) }}</p>

<p>{{ 'studio.email.concluded.hash'|trans({'{hash}': contract.contentHash|slice(0, 16)}) }}</p>
