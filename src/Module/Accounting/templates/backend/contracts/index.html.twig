{% extends '@Core/backend/layout.html.twig' %}

{% block title %}{{ 'backend.nav.accounting_contracts'|trans }} - {{ parent() }}{% endblock %}

{% block page_header_slot %}
    {{ include('@Shared/components/page_header.html.twig', {
        crumbs: [
            {label: 'backend.nav.sections.accounting'|trans},
            {label: 'backend.nav.accounting_contracts'|trans},
        ],
    }) }}
{% endblock %}

{% block body %}
<div {{ vue_component('accounting/backend/contracts/ContractsApp', {
    contracts: contracts,
    customers: customers,
    bodies: bodies,
    annexes: annexes,
    locales: locales,
    currencies: currencies,
    createPath: createPath,
    updatePath: updatePath,
    deletePath: deletePath,
    freezePath: freezePath,
    sendPath: sendPath,
    revokeLinkPath: revokeLinkPath,
    countersignPath: countersignPath,
    showPath: showPath,
}) }} class="flex-1 min-w-0"></div>
{% endblock %}
